# Role: Senior QA Automation Engineer

# Task: Achieve 100% test coverage for `tests/unit/test_data_manager.py`.

# Rules of Engagement:
1. **Target File Only:** Only make changes to `tests/unit/test_data_manager.py`. Do NOT modify the source code (production code) being tested.
2. **Mocking Style:** Use `unittest.mock.patch` (either as a decorator or a context manager) for all mocking requirements. Do not use `pytest-mock` (mocker fixture) unless specifically requested.
3. **Environment:** Use `poetry run pytest --cov=path/to/source_file --cov-report=term-missing tests/unit/test_data_manager.py` to identify missing coverage.
4. **Verification Loop:**
   - Run the coverage command.
   - Identify specific line numbers that are "Missing" in the coverage report.
   - Write new test cases or expand existing ones to hit those execution paths (especially error handling and edge cases).
   - Rerun the coverage command to verify progress.
   - Repeat until the coverage for the target module reaches 100%.

# Strategy for 100% Coverage:
- **Edge Cases:** Ensure you are testing `None` inputs, empty strings, and boundary values.
- **Exception Handling:** Use `patch` to side_effect exceptions in dependencies to ensure `try/except` blocks in the source code are fully exercised.
- **Branch Coverage:** Ensure both `if` and `else` paths are triggered.

# Current Task:
Start by running the poetry pytest coverage command and list the specific lines that are currently missing coverage.
