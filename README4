# Task: Step 1 - Identify Coverage Gaps

Let's break this down into smaller steps to prevent tool execution errors. Do not attempt to write any code yet.

### Directives:
1. Run this exact command to see what is missing:
   `poetry run pytest --cov=path/to/source_file --cov-report=term-missing tests/unit/test_data_manager.py`
2. Reply with ONLY the specific line numbers that are reported as "Missing" in the terminal output.

Stop after displaying the missing line numbers and wait for my next instruction.

=======================================================================================================================================

# Task: Step 2 - Fix Lines 45-50

Great. Now let's implement a single test case to cover exactly those lines.

### Instructions:
1. Read the production code file where lines 45-50 live so you understand the logic.
2. Open `tests/unit/test_data_manager.py` and write ONE new test function at the bottom of the file that uses `unittest.mock.patch` to force execution through those specific lines.
3. Save the file.
4. Run the coverage command again to confirm those lines are no longer missing.

Do not move on to any other lines until this single edit is successful.

================================================================================================================================================

Resolved the volume mounting disconnect; artifacts (PDFs/CSVs) are now successfully persisting to the G: drive across container teardowns.

The New Issue: File Access Locking & Resource Latency
Now that the app is successfully pointing to the hostdaemon.log, we have encountered a common "Windows-ism": Concurrent Access Conflicts.

The Problem:
Because the hostdaemon on the Windows Server is frequently writing to the log file, the Streamlit app occasionally hits a PermissionError (Error 13). Windows is locking the file for writing, which prevents our Python regex engine from opening it for reading. Furthermore, as the log grows in size, performing a full regex sweep on every UI interaction is causing Streamlit to lag.

Solutions in Development
To keep the search utility responsive and "crash-proof," I am implementing the following logic:

1. Non-Blocking Read (The "Shadow Copy" Method)
Instead of opening the active log directly, the utility will now utilize a temporary buffer or the shutil.copy2 method. This allows us to read a "snapshot" of the log without triggering a file-lock conflict with the host daemon.
